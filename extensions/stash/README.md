# Stash

Ctrl+S moves editor text into a scratch buffer and clears the editor. Press it
again to restore the text. If both contain text, it swaps them. The buffer lasts
until Pi reloads or exits; it is not written to disk.
